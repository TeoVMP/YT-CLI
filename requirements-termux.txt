# Requirements for Termux (Android)
# 
# IMPORTANT: Install cryptography FIRST from Termux packages:
#   pkg install py312-cryptography  (or py311-cryptography, py310-cryptography, etc.)
#
# Then install these requirements:
#   pip install -r requirements-termux.txt

# YouTube Bot Dependencies
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
python-dotenv>=1.0.0

# For video/audio downloading
yt-dlp>=2023.12.30
mutagen>=1.47.0

# Note: cryptography is NOT included here because it must be installed
# from Termux packages (pkg install py312-cryptography) to avoid compilation issues.
